# Own-decoder round trip: encode tetrahedron.obj with draco-oxide and decode it with
# draco-oxide, bounding the decoded surface's drift from the source. Runs
# without the Google Draco binaries, so this coverage never skips.

[[operations]]
op = "DracoOxideEncode"
input = "tetrahedron.obj"
output = "oxide.drc"

[[operations]]
op = "DracoOxideDecode"
input = "oxide.drc"
output = "decoded.obj"

[[operations]]
op = "Validation"
input = "decoded.obj"
fmt = "Obj"

[[operations]]
op = "Comparison"
input1 = "tetrahedron.obj"
input2 = "decoded.obj"
methods = [{ method = "L2Norm", max = 0.0035 }]
