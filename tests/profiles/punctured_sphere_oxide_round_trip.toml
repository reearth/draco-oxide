# Own-decoder round trip: encode punctured_sphere.obj with draco-oxide and decode it with
# draco-oxide, bounding the decoded surface's drift from the source. Runs
# without the Google Draco binaries, so this coverage never skips.

[[operations]]
op = "DracoOxideEncode"
input = "punctured_sphere.obj"
output = "oxide.drc"

[[operations]]
op = "DracoOxideDecode"
input = "oxide.drc"
output = "decoded.obj"

[[operations]]
op = "Validation"
input = "decoded.obj"
fmt = "Obj"

[[operations]]
op = "Comparison"
input1 = "punctured_sphere.obj"
input2 = "decoded.obj"
methods = [{ method = "L2Norm", max = 0.035 }]
